add in pre-build build event to check for XPression



All XPression features are now skipped when the program is build without XPression.dll.
This does not happen when application is turned into .exe